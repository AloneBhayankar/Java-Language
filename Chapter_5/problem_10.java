// 10. A do-while loop is executed:
// At least once | At least twice | At most once
// Output:  ✅ At least once 
// A do...while loop always executes at least once because the condition is checked after the first execution.